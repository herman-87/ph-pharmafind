<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="008-seed-default-roles" author="codex">
        <sql>
            INSERT INTO t_role (c_id, c_name)
            SELECT '00000000-0000-0000-0000-000000000101', 'ROLE_USER'
            WHERE NOT EXISTS (SELECT 1 FROM t_role WHERE c_name = 'ROLE_USER');
        </sql>

        <sql>
            INSERT INTO t_role (c_id, c_name)
            SELECT '00000000-0000-0000-0000-000000000102', 'ROLE_ADMIN'
            WHERE NOT EXISTS (SELECT 1 FROM t_role WHERE c_name = 'ROLE_ADMIN');
        </sql>
    </changeSet>

</databaseChangeLog>
